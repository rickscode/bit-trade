{
  "strategy_type": "volatility_trading",
  "indicators": [
    "CCI",
    "OBV",
    "Bollinger Bands",
    "ADX"
  ],
  "risk_style": "fixed_percentage",
  "generation_timestamp": "2025-07-10T13:09:32.504344",
  "learning_version": "v2.2_q_learning",
  "model_used": "versatile",
  "market_conditions": {
    "volatility": 0.02772988289058852,
    "trend": 1.0993928186230957,
    "recent_performance": -0.0725374881417587
  },
  "model_metadata": {
    "model_key": "versatile",
    "model_name": "llama-3.3-70b-versatile",
    "timestamp": "2025-07-10T13:09:32.504326"
  },
  "content": "```json\n{\n  \"strategy_type\": \"Volatility Trading\",\n  \"indicators\": [\n    {\n      \"name\": \"CCI\",\n      \"period\": 14\n    },\n    {\n      \"name\": \"OBV\",\n      \"period\": 20\n    },\n    {\n      \"name\": \"Bollinger Bands\",\n      \"period\": 20,\n      \"std_dev\": 2\n    },\n    {\n      \"name\": \"ADX\",\n      \"period\": 14\n    }\n  ],\n  \"entry_rules\": [\n    {\n      \"long\": \"CCI crosses above -100 and OBV is above its 20-period average and price is above the lower Bollinger Band and ADX is above 20\"\n    },\n    {\n      \"short\": \"CCI crosses below 100 and OBV is below its 20-period average and price is below the upper Bollinger Band and ADX is above 20\"\n    }\n  ],\n  \"exit_rules\": [\n    {\n      \"long\": \"CCI crosses below -100 or price reaches the upper Bollinger Band or ADX falls below 20\"\n    },\n    {\n      \"short\": \"CCI crosses above 100 or price reaches the lower Bollinger Band or ADX falls below 20\"\n    }\n  ],\n  \"risk_management\": {\n    \"type\": \"fixed_percentage\",\n    \"percentage\": 0.02\n  },\n  \"position_sizing\": {\n    \"type\": \"fixed_fraction\",\n    \"fraction\": 0.1\n  },\n  \"stop_loss\": {\n    \"type\": \"percentage\",\n    \"percentage\": 0.05\n  },\n  \"take_profit\": {\n    \"type\": \"percentage\",\n    \"percentage\": 0.1\n  },\n  \"code\": \"\nimport pandas as pd\nimport numpy as np\n\n# Define indicators\ndef calculate_cci(data, period):\n    cci = (data['close'] - data['close'].rolling(window=period).mean()) / (0.015 * data['close'].rolling(window=period).std())\n    return cci\n\ndef calculate_obv(data, period):\n    obv = np.where(data['close'] > data['close'].shift(1), data['volume'], -data['volume']).cumsum()\n    obv_avg = obv.rolling(window=period).mean()\n    return obv, obv_avg\n\ndef calculate_bollinger_bands(data, period, std_dev):\n    sma = data['close'].rolling(window=period).mean()\n    std = data['close'].rolling(window=period).std()\n    upper_band = sma + std_dev * std\n    lower_band = sma - std_dev * std\n    return upper_band, lower_band\n\ndef calculate_adx(data, period):\n    high_low = data['high'] - data['low']\n    high_close = np.abs(data['high'] - data['close'].shift(1))\n    low_close = np.abs(data['low'] - data['close'].shift(1))\n    ranges = pd.concat([high_low, high_close, low_close], axis=1)\n    true_range = ranges.max(axis=1)\n    atr = true_range.rolling(window=period).mean()\n    return atr\n\n# Define strategy\ndef volatility_trading_strategy(data):\n    cci = calculate_cci(data, 14)\n    obv, obv_avg = calculate_obv(data, 20)\n    upper_band, lower_band = calculate_bollinger_bands(data, 20, 2)\n    adx = calculate_adx(data, 14)\n\n    long_signals = np.where((cci > -100) & (obv > obv_avg) & (data['close'] > lower_band) & (adx > 20), 1, 0)\n    short_signals = np.where((cci < 100) & (obv < obv_avg) & (data['close'] < upper_band) & (adx > 20), -1, 0)\n\n    signals = long_signals + short_signals\n\n    return signals\n\n# Define risk management\ndef fixed_percentage_risk_management(data, signals, percentage):\n    positions = np.where(signals == 1, data['close'] * percentage, 0)\n    return positions\n\n# Define position sizing\ndef fixed_fraction_position_sizing(data, signals, fraction):\n    positions = np.where(signals == 1, data['close'] * fraction, 0)\n    return positions\n\n# Define stop loss and take profit\ndef stop_loss_and_take_profit(data, signals, stop_loss_percentage, take_profit_percentage):\n    stop_loss = data['close'] * (1 - stop_loss_percentage)\n    take_profit = data['close'] * (1 + take_profit_percentage)\n    return stop_loss, take_profit\n\n# Backtest strategy\ndef backtest_strategy(data):\n    signals = volatility_trading_strategy(data)\n    positions = fixed_fraction_position_sizing(data, signals, 0.1)\n    stop_loss, take_profit = stop_loss_and_take_profit(data, signals, 0.05, 0.1)\n\n    # Calculate performance metrics\n    wins = np.sum(signals == 1)\n    losses = np.sum(signals == -1)\n    win_rate = wins / (wins + losses)\n    max_drawdown = np.max(np.cumsum(signals) - np.maximum.accumulate(np.cumsum(signals)))\n    total_return = np.sum(signals) / len(data)\n\n    return win_rate, max_drawdown, total_return\n\n# Load data\ndata = pd.read_csv('data.csv')\n\n# Backtest strategy\nwin_rate, max_drawdown, total_return = backtest_strategy(data)\n\nprint('Win Rate: ', win_rate)\nprint('Max Drawdown: ', max_drawdown)\nprint('Total Return: ', total_return)\n  \"\n}\n```"
}