{
  "strategy_type": "volatility_trading",
  "indicators": [
    "Williams %R",
    "MACD",
    "CCI"
  ],
  "risk_style": "position_sizing",
  "generation_timestamp": "2025-07-10T13:06:13.791506",
  "learning_version": "v2.2_q_learning",
  "model_used": "versatile",
  "market_conditions": {
    "volatility": 0.01935640627104757,
    "trend": 0.220669560754196,
    "recent_performance": 0.018124013772460004
  },
  "model_metadata": {
    "model_key": "versatile",
    "model_name": "llama-3.3-70b-versatile",
    "timestamp": "2025-07-10T13:06:13.791483"
  },
  "content": "```json\n{\n  \"strategy_type\": \"Volatility Trading Strategy\",\n  \"indicators\": {\n    \"Williams %R\": {\n      \"period\": 14\n    },\n    \"MACD\": {\n      \"fast_period\": 12,\n      \"slow_period\": 26,\n      \"signal_period\": 9\n    },\n    \"CCI\": {\n      \"period\": 20\n    }\n  },\n  \"entry_rules\": {\n    \"long_entry\": {\n      \"conditions\": [\n        \"Williams %R is oversold (< -80)\",\n        \"MACD line crosses above signal line\",\n        \"CCI is below -100\"\n      ]\n    },\n    \"short_entry\": {\n      \"conditions\": [\n        \"Williams %R is overbought (> -20)\",\n        \"MACD line crosses below signal line\",\n        \"CCI is above 100\"\n      ]\n    }\n  },\n  \"exit_rules\": {\n    \"long_exit\": {\n      \"conditions\": [\n        \"Williams %R is overbought (> -20)\",\n        \"MACD line crosses below signal line\",\n        \"CCI is above 100\"\n      ]\n    },\n    \"short_exit\": {\n      \"conditions\": [\n        \"Williams %R is oversold (< -80)\",\n        \"MACD line crosses above signal line\",\n        \"CCI is below -100\"\n      ]\n    }\n  },\n  \"risk_management\": {\n    \"position_sizing\": {\n      \"type\": \"fixed_fraction\",\n      \"fraction\": 0.02\n    },\n    \"stop_loss\": {\n      \"type\": \"percentage\",\n      \"percentage\": 2\n    },\n    \"take_profit\": {\n      \"type\": \"percentage\",\n      \"percentage\": 5\n    }\n  },\n  \"code\": \"\nimport pandas as pd\nimport numpy as np\n\n# Define indicators\ndef williams_r(high, low, close, period):\n    highest_high = high.rolling(window=period).max()\n    lowest_low = low.rolling(window=period).min()\n    wr = -100 * ((highest_high - close) / (highest_high - lowest_low))\n    return wr\n\ndef macd(line, signal, fast_period, slow_period, signal_period):\n    ema_fast = line.ewm(span=fast_period, adjust=False).mean()\n    ema_slow = line.ewm(span=slow_period, adjust=False).mean()\n    macd_line = ema_fast - ema_slow\n    signal_line = macd_line.ewm(span=signal_period, adjust=False).mean()\n    return macd_line, signal_line\n\ndef cci(high, low, close, period):\n    tp = (high + low + close) / 3\n    cci = (tp - tp.rolling(window=period).mean()) / (0.015 * tp.rolling(window=period).std())\n    return cci\n\n# Define strategy\ndef strategy(data):\n    # Calculate indicators\n    wr = williams_r(data['High'], data['Low'], data['Close'], 14)\n    macd_line, signal_line = macd(data['Close'], data['Close'], 12, 26, 9)\n    cci_line = cci(data['High'], data['Low'], data['Close'], 20)\n\n    # Define entry and exit rules\n    long_entry = (wr < -80) & (macd_line > signal_line) & (cci_line < -100)\n    short_entry = (wr > -20) & (macd_line < signal_line) & (cci_line > 100)\n    long_exit = (wr > -20) & (macd_line < signal_line) & (cci_line > 100)\n    short_exit = (wr < -80) & (macd_line > signal_line) & (cci_line < -100)\n\n    # Define position sizing\n    position_size = 0.02\n\n    # Define stop loss and take profit\n    stop_loss = 0.02\n    take_profit = 0.05\n\n    # Generate signals\n    signals = pd.DataFrame(index=data.index)\n    signals['long_entry'] = long_entry\n    signals['short_entry'] = short_entry\n    signals['long_exit'] = long_exit\n    signals['short_exit'] = short_exit\n\n    return signals\n\n# Test strategy\ndata = pd.read_csv('market_data.csv')\nsignals = strategy(data)\nprint(signals)\n\"\n}\n```\n\nThis JSON object defines a complete trading strategy with clear logic and code. The strategy uses the Williams %R, MACD, and CCI indicators to generate buy and sell signals. The entry and exit rules are based on the values of these indicators, and the position sizing is fixed at 2% of the total equity. The stop loss and take profit levels are set at 2% and 5% respectively.\n\nThe code section defines the indicators and the strategy logic, and generates signals based on the market data. The `williams_r`, `macd`, and `cci` functions calculate the values of the respective indicators, and the `strategy` function defines the entry and exit rules and generates the signals.\n\nThe strategy can be tested by running the code with historical market data, and the performance can be evaluated using metrics such as the Sharpe ratio, win rate, and drawdown.\n\n**Reasoning:**\n\n* The Williams %R indicator is used to identify overbought and oversold conditions in the market.\n* The MACD indicator is used to identify trends and crossovers, which can be used to generate buy and sell signals.\n* The CCI indicator is used to identify extreme values in the market, which can be used to generate buy and sell signals.\n* The position sizing is fixed at 2% to limit the risk of each trade.\n* The stop loss and take profit levels are set at 2% and 5% respectively to limit the risk of each trade and to lock in profits.\n* The strategy is designed to be flexible and can be adjusted based on the market conditions and the performance of the strategy.\n\n**Example Use Cases:**\n\n* The strategy can be used to trade stocks, futures, or forex markets.\n* The strategy can be used to generate buy and sell signals for a portfolio of assets.\n* The strategy can be used to evaluate the performance of a portfolio of assets and to identify areas for improvement.\n* The strategy can be used to develop a trading plan and to make informed investment decisions."
}