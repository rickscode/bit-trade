{
  "strategy_type": "multi_timeframe",
  "indicators": [
    "MACD",
    "ADX",
    "Volume Profile"
  ],
  "risk_style": "kelly_criterion",
  "generation_timestamp": "2025-07-10T13:03:58.818835",
  "learning_version": "v2.2_q_learning",
  "model_used": "versatile",
  "market_conditions": {
    "volatility": 0.02772988289058852,
    "trend": 1.0993928186230957,
    "recent_performance": -0.0725374881417587
  },
  "model_metadata": {
    "model_key": "versatile",
    "model_name": "llama-3.3-70b-versatile",
    "timestamp": "2025-07-10T13:03:58.818814"
  },
  "content": "```json\n{\n  \"strategy_type\": \"Multi-Timeframe Trading Strategy\",\n  \"indicators\": [\n    {\n      \"name\": \"MACD\",\n      \"params\": {\n        \"fast_ma\": 12,\n        \"slow_ma\": 26,\n        \"signal_ma\": 9\n      }\n    },\n    {\n      \"name\": \"ADX\",\n      \"params\": {\n        \"length\": 14\n      }\n    },\n    {\n      \"name\": \"Volume Profile\",\n      \"params\": {\n        \"length\": 20\n      }\n    }\n  ],\n  \"entry_rules\": [\n    {\n      \"rule\": \"Long Entry\",\n      \"conditions\": [\n        {\n          \"indicator\": \"MACD\",\n          \"condition\": \"crossover above signal line\"\n        },\n        {\n          \"indicator\": \"ADX\",\n          \"condition\": \"above 20\"\n        },\n        {\n          \"indicator\": \"Volume Profile\",\n          \"condition\": \"volume above average\"\n        }\n      ]\n    },\n    {\n      \"rule\": \"Short Entry\",\n      \"conditions\": [\n        {\n          \"indicator\": \"MACD\",\n          \"condition\": \"crossover below signal line\"\n        },\n        {\n          \"indicator\": \"ADX\",\n          \"condition\": \"above 20\"\n        },\n        {\n          \"indicator\": \"Volume Profile\",\n          \"condition\": \"volume above average\"\n        }\n      ]\n    }\n  ],\n  \"exit_rules\": [\n    {\n      \"rule\": \"Long Exit\",\n      \"conditions\": [\n        {\n          \"indicator\": \"MACD\",\n          \"condition\": \"crossover below signal line\"\n        },\n        {\n          \"indicator\": \"ADX\",\n          \"condition\": \"below 20\"\n        }\n      ]\n    },\n    {\n      \"rule\": \"Short Exit\",\n      \"conditions\": [\n        {\n          \"indicator\": \"MACD\",\n          \"condition\": \"crossover above signal line\"\n        },\n        {\n          \"indicator\": \"ADX\",\n          \"condition\": \"below 20\"\n        }\n      ]\n    }\n  ],\n  \"risk_management\": {\n    \"type\": \"Kelly Criterion\",\n    \"params\": {\n      \"probability\": 0.6,\n      \"odds\": 1.2,\n      \"bankroll\": 10000\n    }\n  },\n  \"code\": \"\nimport pandas as pd\nimport numpy as np\n\nclass TradingStrategy:\n    def __init__(self, data):\n        self.data = data\n        self.macd = self.calculate_macd()\n        self.adx = self.calculate_adx()\n        self.volume_profile = self.calculate_volume_profile()\n        self.position_size = self.calculate_position_size()\n\n    def calculate_macd(self):\n        # Calculate MACD\n        self.data['ema_12'] = self.data['Close'].ewm(span=12, adjust=False).mean()\n        self.data['ema_26'] = self.data['Close'].ewm(span=26, adjust=False).mean()\n        self.data['macd'] = self.data['ema_12'] - self.data['ema_26']\n        self.data['signal'] = self.data['macd'].ewm(span=9, adjust=False).mean()\n        return self.data[['macd', 'signal']]\n\n    def calculate_adx(self):\n        # Calculate ADX\n        self.data['high_low'] = self.data['High'] - self.data['Low']\n        self.data['high_close'] = np.abs(self.data['High'] - self.data['Close'].shift(1))\n        self.data['low_close'] = np.abs(self.data['Low'] - self.data['Close'].shift(1))\n        self.data['true_range'] = np.maximum(self.data['high_low'], np.maximum(self.data['high_close'], self.data['low_close']))\n        self.data['adx'] = self.data['true_range'].ewm(span=14, adjust=False).mean()\n        return self.data['adx']\n\n    def calculate_volume_profile(self):\n        # Calculate Volume Profile\n        self.data['volume_ma'] = self.data['Volume'].rolling(window=20).mean()\n        return self.data['volume_ma']\n\n    def calculate_position_size(self):\n        # Calculate Position Size using Kelly Criterion\n        probability = 0.6\n        odds = 1.2\n        bankroll = 10000\n        position_size = (probability * odds - (1 - probability)) * bankroll\n        return position_size\n\n    def check_entry(self):\n        # Check for long entry\n        if (self.macd['macd'] > self.macd['signal']) and (self.adx > 20) and (self.data['Volume'] > self.volume_profile):\n            return 'Long'\n        # Check for short entry\n        elif (self.macd['macd'] < self.macd['signal']) and (self.adx > 20) and (self.data['Volume'] > self.volume_profile):\n            return 'Short'\n        else:\n            return None\n\n    def check_exit(self):\n        # Check for long exit\n        if (self.macd['macd'] < self.macd['signal']) and (self.adx < 20):\n            return 'Long Exit'\n        # Check for short exit\n        elif (self.macd['macd'] > self.macd['signal']) and (self.adx < 20):\n            return 'Short Exit'\n        else:\n            return None\n\n# Example usage\ndata = pd.read_csv('data.csv')\nstrategy = TradingStrategy(data)\nentry = strategy.check_entry()\nexit = strategy.check_exit()\nif entry == 'Long':\n    print('Long Entry')\n    # Set stop-loss and take-profit levels\n    stop_loss = data['Low'] - (data['High'] - data['Low']) * 0.1\n    take_profit = data['High'] + (data['High'] - data['Low']) * 0.1\n    print('Stop-Loss:', stop_loss)\n    print('Take-Profit:', take_profit)\nelif entry == 'Short':\n    print('Short Entry')\n    # Set stop-loss and take-profit levels\n    stop_loss = data['High'] + (data['High'] - data['Low']) * 0.1\n    take_profit = data['Low'] - (data['High'] - data['Low']) * 0.1\n    print('Stop-Loss:', stop_loss)\n    print('Take-Profit:', take_profit)\nelif exit == 'Long Exit':\n    print('Long Exit')\nelif exit == 'Short Exit':\n    print('Short Exit')\n\"\n}\n```\n\n**Reasoning:**\n\nThe provided strategy uses a combination of technical indicators, including MACD, ADX, and Volume Profile, to generate buy and sell signals. The MACD indicator is used to identify crossovers above or below the signal line, which can indicate trend reversals. The ADX indicator is used to measure the strength of the trend, with values above 20 indicating a strong trend. The Volume Profile indicator is used to confirm the trade by checking if the volume is above the average.\n\nThe Kelly Criterion is used for risk management, which calculates the optimal position size based on the probability of winning, the odds of winning, and the bankroll.\n\nThe strategy includes specific entry and exit rules, with stop-loss and take-profit levels set based on the current price and volatility. The position sizing logic is also included, which calculates the optimal position size based on the Kelly Criterion.\n\nThe code is written in Python and uses the Pandas library for data manipulation and analysis. The strategy is implemented as a class, with methods for calculating the MACD, ADX, and Volume Profile indicators, as well as for checking entry and exit conditions.\n\n**Example Use Case:**\n\nTo use this strategy, you would need to provide historical market data, including the Open, High, Low, Close, and Volume values for each period. You would then create an instance of the TradingStrategy class, passing in the data, and call the check_entry and check_exit methods to generate buy and sell signals. You would also need to set the stop-loss and take-profit levels based on the current price and volatility.\n\nNote: This is a basic example and you should do your own research and testing before using any trading strategy in a live market."
}